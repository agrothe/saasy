@page "~/{locale=en-us}/{tenant}/account/lockout"
@inject IViewLocalizer Localizer
@model LockoutModel
@{
    ViewData["Title"] = Localizer["LockedOut"];
}

<header>
    <h1 class="text-danger">@ViewData["Title"]</h1>
    <p class="text-danger">@Localizer["AccountIsLockedOut"]</p>
</header>
